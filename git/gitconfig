[user]
	name = Ryan Hedges
	email = ryanhedges@gmail.com

[github]
  name = RyanHedges

[color]
  ui = auto

[core]
  editor = vim
	excludesfile = ~/.gitignore

[status]
  submoduleSummary = true

[diff]
  submodule = log

[alias]
  l = log --graph --oneline --topo-order
  la = !git l --all
	co = checkout
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
