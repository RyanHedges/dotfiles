[user]
	name = Ryan Hedges
	email = ryanhedges@gmail.com
	signingkey = /Users/ryanhedges/.ssh/id_ed25519.pub

[github]
  name = RyanHedges

[color]
  ui = auto

[core]
  editor = vim
	excludesfile = ~/.gitignore

[status]
  submoduleSummary = true

[diff]
  submodule = log

[alias]
  l = log --graph --oneline --topo-order
  la = !git l --all
	co = checkout
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[gpg]
	format = ssh
[commit]
	gpgsign = true
